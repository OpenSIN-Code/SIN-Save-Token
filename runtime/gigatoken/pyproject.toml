[project]
name = "sin-gigatoken-runtime"
version = "0.1.0"
description = "Locked isolated runtime for SIN Gigatoken adapters"
requires-python = ">=3.10"
dependencies = [
  "gigatoken==0.9.0",
]

[dependency-groups]
validation = [
  "tokenizers==0.22.2",
]
